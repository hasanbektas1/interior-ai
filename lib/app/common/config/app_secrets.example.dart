/// Template for local secrets. Copy this file to `app_secrets.dart`
/// (same folder) and fill in your own values. `app_secrets.dart` is
/// git-ignored so your keys never get committed.
class AppSecrets {
  static const String cloudflareAccountId = '';
  static const String cloudflareApiToken = '';
}
